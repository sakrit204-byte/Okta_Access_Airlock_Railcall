Quarterly access review, airlocked. Fourteen nodes. Refuses to start when a scope it needs is missing or when rate limit headroom cannot finish the run, because a review that silently skips a section is worse than one that will not begin. Produces the standing access pack, detects access changes in Okta's System Log with no governed approval behind them, and closes with a custody verdict bounded by the log watermark it was computed against. Removals run only for user ids a human places in the run context, and the write is pinned to an approved plan fingerprint that refuses if the group moved after approval. Requires sakrit204/okta_access_airlock. contest:round2
